<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.fefu.activitytracker">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:name=".screens.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Activity"
        android:requestLegacyExternalStorage="true">
        <activity
            android:name=".screens.activities.NavigationBarActivity"
            android:exported="true">
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>
        <activity
            android:name=".screens.welcome.WelcomeScreenActivity"
            android:exported="true"/>
        <activity
            android:name=".screens.login.LoginScreenActivity"
            android:exported="true" />
        <activity
            android:name=".screens.registration.RegistrationScreenActivity"
            android:exported="true" />
        <activity
            android:name=".screens.map.MapActivity"
            android:exported="true"/>

        <service android:name=".screens.activities.ActivityService"
            android:foregroundServiceType="location"/>
    </application>

</manifest>